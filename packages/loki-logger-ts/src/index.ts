import { LokiClient } from "./LokiClient";
import {
  Log,
  LogDebug,
  LogError,
  LogInfo,
  LogWarning,
  LokiLabels,
} from "./Logger";

export { LokiClient, Log, LogDebug, LogError, LogInfo, LogWarning };
export type { LokiLabels };
