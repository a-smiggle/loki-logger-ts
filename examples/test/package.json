{
  "name": "test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "nodemon --watch 'src/*.ts' --exec ts-node src/index.ts",
    "lint": "eslint \"src/**/*.ts*\""
  },
  "dependencies": {
    "loki-logger-ts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^17.0.12",
    "eslint-config-custom": "workspace:*",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "tsconfig": "workspace:*",
    "typescript": "^4.5.3"
  }
}
